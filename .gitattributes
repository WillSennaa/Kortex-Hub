# Força o arquivo do StarUML a ser tratado como binário ou texto sem conversão de quebra de linha
*.mdj binary

# Garante que arquivos de código (C#) mantenham um padrão saudável
*.cs text eol=crlf
*.json text eol=lf
